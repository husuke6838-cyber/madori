-- =========================================================
-- ローカル開発用シード（`npx supabase db reset` 時に自動実行）
-- 本番では実行しない。
--
-- 注意: ローカルの supabase はデモ用ユーザを自動作成しないので、
-- 開発時はサインアップで自分のユーザを作ってから動作確認すること。
-- 部屋テンプレ／検討ヒント／後悔ポイントは lib/seeds.ts のデータを
-- サーバアクションで投入する設計（プロジェクトごとに毎回挿入する）。
-- =========================================================

-- 何もしない（プレースホルダ）。必要に応じてデモプロジェクトを挿入する。
select 1;
